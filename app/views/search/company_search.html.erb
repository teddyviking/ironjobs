<h1>Search companies</h1>
	<%= render 'form' %>
<ul class="post-search">
	<% @companies.each do |company| %>
		<li> <%= link_to company.company_name, company_path(company)%> </br>
			tags: <%= company.tag_list %></li>
	<% end %>
</ul>