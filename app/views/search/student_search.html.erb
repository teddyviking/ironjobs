<h1>Search students</h1>
	<%= render 'form' %>
<ul class="post-search">
	<% @students.each do |student| %>
		<li> <%= link_to student.name, student_path(student) %> </br>
			tags: <%= student.tag_list %></li>
	<% end %>
</ul>
